
const state = {}
const types = {
    GO_PAGES: "GO_PAGES"
}
const mutations = {
    // [types.GO_PAGES](url) {
    //     console.log(this);
    //     // Vue.$router.push(url);
    // }
}
const actions = {
    //ES5参数解构 
    // goPages({ commit }) {
    //     commit(types.GO_PAGES);
    // }
}
export default {
    state,
    actions,
    mutations
}